# security-context-lab-duo-api

Synthetic security test fixture. Never deploy publicly or use real data.

Python 3.10+, standard library only. `server.py` contains this service boundary.
Calls use injected service modules; the external local runner composes them.

System peers: security-context-lab-duo-gateway
